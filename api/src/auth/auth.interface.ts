import { UserTier } from './../user/user.interface';

export const publicEndpointMetadata = 'PUBLIC_ENDPOINT';
export const permissionMetadata = 'PERMISSION_ENDPOINT';

export type Permission = UserTier;
